# ───── Build output ─────
bin/
obj/
[Dd]ebug/
[Rr]elease/
x64/
x86/
[Aa][Rr][Mm]/
[Aa][Rr][Mm]64/
publish/

# Release artifacts produced by build-release.ps1
dist/

# ───── Visual Studio / Rider / VS Code ─────
.vs/
.vscode/
.idea/
*.user
*.suo
*.userosscache
*.sln.docstates
*_i.c
*_p.c

# ───── .NET ─────
project.lock.json
project.fragment.lock.json
artifacts/
*.nupkg
*.snupkg
msbuild.log
msbuild.err
msbuild.wrn

# ───── Test results ─────
[Tt]est[Rr]esult*/
[Bb]uild[Ll]og.*
coverage*.json
coverage*.xml
coverage*.info
*.coverage
*.coveragexml

# ───── OS / misc ─────
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
*.tmp
*.log

# ───── App runtime data (never commit secrets) ─────
# Local vault and preferences live under %AppData%\PasswordKeeper, not the repo,
# but guard against stray copies landing in the working tree.
vault.json
preferences.json
*.vault

design/
dist/
.claude/